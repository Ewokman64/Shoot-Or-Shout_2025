using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RangedEnemy : MonoBehaviour
{
    // Variable to hold the associated spawn point
    public Transform associatedSpawnPoint;

    // Add any other properties or methods relevant to your RangedEnemy class
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
